global class KeyContactController {
/*
    // the actual account
    public Account a; 
 
    public ID [] totalAccounts = new ID []{};
    public integer counter = 0;
    public integer limits =5;
    public integer recCount = 0;
    public string parentId { get{return a.id;} private set;}
    
    public KeyContactController(ApexPages.StandardController controller) {
        
        system.debug('in constructor :');        
        this.a = (Account) controller.getRecord();   
        
        string prntId = parentId;

    }

    

    @RemoteAction
    global static Id getAllContactReportId(){

        Id result;

        Report allContacts = [SELECT Id, Name, DeveloperName
                              FROM Report
                              WHERE DeveloperName = 'All_Account_Contacts'
                              Limit 1];

        if(allContacts != null){
            result = allContacts.Id;
        }
        return result;
    }
    */
}